{% extends 'base.html.twig' %}

{# Paramètre #}
{% set titlebouton = 'Modifier la transaction' %}
{% set icone = 'edit' %}
{% set back = 'app_front_index' %}

{% block title %}Transaction {{ transaction.originalprice|number_format(2, ',', ' ') }}€{% endblock %}

{% block body %}

    {% include('atomic/breadcrump.html.twig') %}

    <div class="slow-background">
        <div class="container">

            <div class="col-start-1 col-end-13  margin-bottom-xxxl">

                {# read and display just one flash message type #}
                {% include('atomic/message_validation.html.twig') %}

                {# Cartouche d'information #}
                {% include('atomic/crypto-info.html.twig') %}

        </div>
    </div>

{% endblock %}